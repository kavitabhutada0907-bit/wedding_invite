import React from "react";
import ReactDOM from "react-dom/client";
import WeddingInvitation from "../wedding_invitation_2";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <WeddingInvitation />
  </React.StrictMode>
);
